const React = require('react');

const SuperAdminDashboard = () => {
  return (
    <>
      <div>
        {true && (
          true ? (
            <div />
          )}
          {false && (
            <div />
          )}
      </div>
      {/* comment */}
    </>
  );
};
